# Worker endpoint namespace v1 vectors

Owner: AI-Ascension/ascension-watchdog. License: MIT. These original synthetic
vectors encode the owner launch-boundary contract in `docs/worker-endpoint-v1.md`.
They contain no process authority, credentials, user paths, or game material.

`conformance.json` SHA-256:
`2f7d9758a681ed77d633e3ed576de1a415734b7ef96255f099e0cb6c9b658f41`.

`expected: null` means the nonce or namespace must be rejected before IPC. The
harness independently consumes the Linux subset; Windows vectors do not establish
a harness Windows listener implementation. The exact file bytes are mirrored as
a data artifact, never as a sibling implementation-crate dependency.
